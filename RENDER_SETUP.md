# Быстрая настройка на Render

## 🚀 Быстрый старт (через Blueprint)

1. **Загрузите код в GitHub репозиторий**

2. **В Render Dashboard**:
   - Нажмите **New +** → **Blueprint**
   - Подключите ваш GitHub репозиторий
   - Render автоматически создаст все сервисы из `render.yaml`

3. **Настройте переменные окружения**:
   - В каждом сервисе добавьте `TELEGRAM_BOT_TOKEN` (ваш токен бота)
   - В API сервисе добавьте `JWT_SECRET` (сгенерируйте случайную строку)

4. **После первого деплоя**:
   - Откройте Shell в API сервисе
   - Выполните: `cd apps/api && pnpm db:migrate:deploy`

## 📝 Что создается автоматически

- ✅ PostgreSQL база данных (`telegram-shop-db`)
- ✅ API сервер (`telegram-shop-api`)
- ✅ Админ панель (`telegram-shop-admin`)
- ✅ Мини-приложение (`telegram-shop-miniapp`)
- ✅ Telegram бот (`telegram-shop-bot`)

## ⚙️ Важные настройки

### Для всех сервисов:
- **PNPM_VERSION=8** (уже настроено в render.yaml через corepack)

### API сервер:
```
DATABASE_URL=<автоматически из БД>
JWT_SECRET=<сгенерируйте>
TELEGRAM_BOT_TOKEN=<ваш токен>
```

### Admin и MiniApp:
```
NEXT_PUBLIC_API_URL=https://telegram-shop-api.onrender.com
```

### Bot:
```
TELEGRAM_BOT_TOKEN=<ваш токен>
MINI_APP_URL=https://telegram-shop-miniapp.onrender.com
```

## 🔗 URL сервисов

После деплоя ваши сервисы будут доступны по адресам:
- API: `https://telegram-shop-api.onrender.com`
- Admin: `https://telegram-shop-admin.onrender.com`
- MiniApp: `https://telegram-shop-miniapp.onrender.com`

## 📚 Подробная инструкция

См. [DEPLOY.md](./DEPLOY.md) для детальной инструкции по ручной настройке.

