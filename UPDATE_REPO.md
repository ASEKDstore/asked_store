# Инструкция по обновлению репозитория GitHub

## 📋 Что было обновлено

1. ✅ **README.md** - обновлен с информацией о Render деплое
2. ✅ **package.json** - добавлены скрипты для typecheck, lint, migrate:deploy
3. ✅ **turbo.json** - конфигурация Turborepo
4. ✅ **tsconfig.base.json** - базовый TypeScript конфиг
5. ✅ **eslint.config.mjs** - конфигурация ESLint
6. ✅ **render.yaml** - полная конфигурация для Render деплоя
7. ✅ **packages/shared** - создан пакет с общими типами
8. ✅ Все tsconfig.json файлы обновлены для наследования от базового
9. ✅ Добавлены файлы для деплоя: DEPLOY.md, RENDER_SETUP.md, DEVELOPMENT.md, PLAN.md

## 🚀 Как обновить репозиторий

### Вариант 1: Через Git (если репозиторий уже подключен)

```bash
# Проверьте текущий remote
git remote -v

# Если remote не настроен, добавьте:
git remote add origin https://github.com/ASEKDstore/asked_store.git

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Update project structure for Render deployment

- Add render.yaml for automatic deployment
- Update README with Render deployment info
- Add Turborepo configuration
- Add shared types package
- Update all tsconfig files
- Add deployment documentation"

# Отправьте в репозиторий
git push origin main
```

### Вариант 2: Через GitHub Desktop или веб-интерфейс

1. Откройте GitHub Desktop или веб-интерфейс GitHub
2. Добавьте все измененные файлы
3. Создайте коммит с описанием изменений
4. Отправьте (push) изменения в репозиторий

### Вариант 3: Инициализация нового репозитория

Если репозиторий еще не инициализирован локально:

```bash
# Инициализируйте git
git init

# Добавьте remote
git remote add origin https://github.com/ASEKDstore/asked_store.git

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Project structure with Render deployment"

# Отправьте в репозиторий
git branch -M main
git push -u origin main
```

## 📝 Важные файлы для проверки

Убедитесь, что следующие файлы присутствуют:

- ✅ `README.md` - обновлен
- ✅ `package.json` - обновлен со скриптами
- ✅ `render.yaml` - конфигурация Render
- ✅ `turbo.json` - конфигурация Turborepo
- ✅ `tsconfig.base.json` - базовый TypeScript конфиг
- ✅ `eslint.config.mjs` - конфигурация ESLint
- ✅ `.nvmrc` - версия Node.js
- ✅ `packages/shared/` - общие типы
- ✅ `DEPLOY.md` - инструкция по деплою
- ✅ `RENDER_SETUP.md` - быстрая настройка Render

## 🔍 После обновления

После того как файлы загружены в GitHub:

1. **Проверьте в Render Dashboard**:
   - Если Blueprint уже создан, он автоматически обновится
   - Если нет, создайте новый Blueprint из обновленного `render.yaml`

2. **Проверьте переменные окружения**:
   - `TELEGRAM_BOT_TOKEN` должен быть установлен во всех сервисах
   - `JWT_SECRET` должен быть установлен в API

3. **Запустите миграции** (если нужно):
   - Откройте Shell в API сервисе
   - Выполните: `cd apps/api && pnpm db:migrate:deploy`

## ⚠️ Важно

- Не коммитьте файлы `.env` - они уже в `.gitignore`
- Убедитесь, что `pnpm-lock.yaml` обновлен (если есть)
- Проверьте, что все зависимости установлены корректно


