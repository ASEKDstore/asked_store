# Деплой на Render

## 📋 Предварительные требования

1. Аккаунт на [Render](https://render.com)
2. Репозиторий на GitHub
3. Telegram Bot Token (для бота)
4. **Важно**: Render должен поддерживать pnpm. Добавьте в корень проекта файл `.nvmrc` (уже создан) для указания версии Node.js

## 🗄️ Шаг 1: Создание базы данных PostgreSQL

1. В панели Render нажмите **New +** → **PostgreSQL**
2. Настройки:
   - **Name**: `telegram-shop-db`
   - **Database**: `telegram_shop`
   - **User**: `telegram_shop_user`
   - **Plan**: Free (или выберите нужный план)
3. После создания скопируйте **Internal Database URL** (он будет использован автоматически)

## 🔧 Шаг 2: Настройка API сервера

1. В панели Render нажмите **New +** → **Web Service**
2. Подключите ваш GitHub репозиторий
3. **Важно**: В настройках сервиса добавьте переменную окружения:
   - **Key**: `PNPM_VERSION`
   - **Value**: `8`
   Это необходимо для установки pnpm на Render
4. Настройки:
   - **Name**: `telegram-shop-api`
   - **Environment**: `Node`
   - **Build Command**: 
     ```bash
     pnpm install && cd apps/api && pnpm build
     ```
   - **Start Command**: 
     ```bash
     cd apps/api && pnpm db:migrate:deploy && pnpm start
     ```
   - **Plan**: Free

4. **Environment Variables**:
   ```
   DATABASE_URL=<Internal Database URL из шага 1>
   NODE_ENV=production
   PORT=10000
   JWT_SECRET=<сгенерируйте случайную строку>
   TELEGRAM_BOT_TOKEN=<ваш токен бота>
   ```

5. **Примечание**: Миграции запускаются автоматически при старте сервиса (через `startCommand`). Если нужно запустить вручную:
   - В разделе **Shell** выполните:
     ```bash
     cd apps/api && pnpm db:migrate:deploy
     ```

## 🎨 Шаг 3: Деплой админ панели

1. **New +** → **Web Service**
2. Подключите тот же GitHub репозиторий
3. **Важно**: Добавьте переменную окружения `PNPM_VERSION=8`
4. Настройки:
   - **Name**: `telegram-shop-admin`
   - **Environment**: `Node`
   - **Build Command**: 
     ```bash
     pnpm install && cd apps/admin && pnpm build
     ```
   - **Start Command**: 
     ```bash
     cd apps/admin && pnpm start
     ```
   - **Plan**: Free

4. **Environment Variables**:
   ```
   NODE_ENV=production
   NEXT_PUBLIC_API_URL=https://telegram-shop-api.onrender.com
   ```

## 📱 Шаг 4: Деплой мини-приложения

1. **New +** → **Web Service**
2. Подключите тот же GitHub репозиторий
3. **Важно**: Добавьте переменную окружения `PNPM_VERSION=8`
4. Настройки:
   - **Name**: `telegram-shop-miniapp`
   - **Environment**: `Node`
   - **Build Command**: 
     ```bash
     pnpm install && cd apps/miniapp && pnpm build
     ```
   - **Start Command**: 
     ```bash
     cd apps/miniapp && pnpm start
     ```
   - **Plan**: Free

4. **Environment Variables**:
   ```
   NODE_ENV=production
   NEXT_PUBLIC_API_URL=https://telegram-shop-api.onrender.com
   ```

5. **Важно**: После деплоя обновите URL мини-приложения в настройках Telegram бота

## 🤖 Шаг 5: Настройка Telegram бота

1. **New +** → **Background Worker**
2. Подключите тот же GitHub репозиторий
3. **Важно**: Добавьте переменную окружения `PNPM_VERSION=8`
4. Настройки:
   - **Name**: `telegram-shop-bot`
   - **Environment**: `Node`
   - **Build Command**: 
     ```bash
     pnpm install && cd apps/bot && pnpm build
     ```
   - **Start Command**: 
     ```bash
     cd apps/bot && pnpm start
     ```
   - **Plan**: Free

4. **Environment Variables**:
   ```
   NODE_ENV=production
   TELEGRAM_BOT_TOKEN=<ваш токен бота>
   MINI_APP_URL=https://telegram-shop-miniapp.onrender.com
   ```

## 🔄 Альтернатива: Использование render.yaml

Если вы используете `render.yaml` (Blueprint), то:

1. В панели Render нажмите **New +** → **Blueprint**
2. Подключите ваш GitHub репозиторий
3. Render автоматически создаст все сервисы из `render.yaml`

**Примечание**: После создания через Blueprint, вам все равно нужно будет:
- Установить переменные окружения `TELEGRAM_BOT_TOKEN` вручную
- Запустить миграции БД через Shell

## 🔐 Настройка переменных окружения

### API (telegram-shop-api)
```
DATABASE_URL=<Internal Database URL>
NODE_ENV=production
PORT=10000
JWT_SECRET=<случайная строка>
TELEGRAM_BOT_TOKEN=<токен бота>
```

### Admin (telegram-shop-admin)
```
NODE_ENV=production
NEXT_PUBLIC_API_URL=https://telegram-shop-api.onrender.com
```

### Mini App (telegram-shop-miniapp)
```
NODE_ENV=production
NEXT_PUBLIC_API_URL=https://telegram-shop-api.onrender.com
```

### Bot (telegram-shop-bot)
```
NODE_ENV=production
TELEGRAM_BOT_TOKEN=<токен бота>
MINI_APP_URL=https://telegram-shop-miniapp.onrender.com
```

## 📝 Настройка Telegram Mini App

После деплоя мини-приложения:

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Выберите вашего бота
3. Отправьте команду `/newapp`
4. Укажите URL вашего мини-приложения: `https://telegram-shop-miniapp.onrender.com`

## 🚨 Важные замечания

1. **Free план на Render**: Сервисы могут "засыпать" после 15 минут бездействия. Первый запрос может занять до 30 секунд.

2. **База данных**: На Free плане БД автоматически удаляется через 90 дней без использования. Для продакшена используйте платный план.

3. **HTTPS**: Render автоматически предоставляет HTTPS для всех сервисов.

4. **Переменные окружения**: Используйте **Sync** для переменных, которые должны быть одинаковыми между сервисами.

5. **Миграции**: Запускайте миграции через Shell после каждого деплоя API, если схема БД изменилась.

## 🔄 Автоматический деплой

Render автоматически деплоит при каждом push в основную ветку (обычно `main` или `master`).

Для отключения автодеплоя:
- В настройках сервиса → **Manual Deploy Only**

## 📊 Мониторинг

- **Logs**: Просмотр логов в реальном времени в панели Render
- **Metrics**: Базовые метрики доступны на Free плане
- **Alerts**: Настройте уведомления о падениях сервисов

## 🐛 Troubleshooting

### API не запускается
- Проверьте логи в панели Render
- Убедитесь, что `DATABASE_URL` установлен правильно
- Проверьте, что миграции выполнены

### Мини-приложение не подключается к API
- Проверьте `NEXT_PUBLIC_API_URL` в переменных окружения
- Убедитесь, что API сервер запущен и доступен

### Бот не отвечает
- Проверьте `TELEGRAM_BOT_TOKEN`
- Убедитесь, что Worker запущен (не Web Service)
- Проверьте логи бота

