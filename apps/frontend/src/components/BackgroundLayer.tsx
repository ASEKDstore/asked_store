import React from 'react'
import './BackgroundLayer.css'

/**
 * Fixed background layer component
 * Doesn't scroll with content, stays fixed in viewport
 */
export const BackgroundLayer: React.FC = () => {
  return <div className="background-layer" />
}

