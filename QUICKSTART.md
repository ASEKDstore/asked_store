# Быстрый старт

## ✅ Что уже готово

1. **Структура monorepo проекта** с 4 приложениями:
   - `apps/admin` - Админ панель (Next.js)
   - `apps/miniapp` - Мини-приложение для пользователей (Next.js)
   - `apps/api` - Backend API (Fastify)
   - `apps/bot` - Telegram бот

2. **База данных (Prisma схема)** с моделями:
   - User (пользователи из Telegram)
   - Product (товары)
   - Category (категории)
   - Order (заказы)
   - Banner (баннеры)
   - Page (страницы)
   - InventoryLog (учет остатков)
   - CartItem (корзина)

3. **Базовая админ панель** с:
   - Layout компонентом с навигацией
   - Главной страницей со статистикой
   - Настроенным Tailwind CSS

4. **API сервер** с базовыми эндпоинтами:
   - `/health` - проверка работы
   - `/api/admin/products` - список товаров
   - `/api/admin/orders` - список заказов
   - `/api/admin/stats` - статистика

## 🚀 Следующие шаги

### Вариант 1: Локальная разработка

#### 1. Установка зависимостей

```bash
pnpm install
```

#### 2. Настройка базы данных

1. Установите PostgreSQL
2. Создайте базу данных:
```sql
CREATE DATABASE telegram_shop;
```

3. Создайте файл `apps/api/.env`:
```env
DATABASE_URL="postgresql://user:password@localhost:5432/telegram_shop?schema=public"
JWT_SECRET="your-secret-key-here"
PORT=3000
```

4. Запустите миграции:
```bash
cd apps/api
pnpm db:migrate
```

### Вариант 2: Деплой на Render (Рекомендуется)

Проект настроен для деплоя на Render через GitHub.

1. **Загрузите код в GitHub репозиторий**
2. **Следуйте инструкциям в [DEPLOY.md](./DEPLOY.md)**
3. Render автоматически создаст все сервисы из `render.yaml`

**Преимущества деплоя на Render:**
- Автоматический деплой при push в GitHub
- Бесплатный план для начала
- Автоматический HTTPS
- Управление переменными окружения через UI

### 3. Запуск в режиме разработки

```bash
# Из корня проекта
pnpm dev
```

Или по отдельности:
- Админ панель: `pnpm dev:admin` (порт 3001)
- Мини-приложение: `pnpm dev:miniapp` (порт 3002)
- API: `pnpm dev:api` (порт 3000)
- Бот: `pnpm dev:bot`

## 📋 План разработки

Подробный план находится в файле `PLAN.md`. 

**Рекомендуемый порядок:**
1. ✅ Базовая структура (готово)
2. ⏳ Настройка БД и миграции
3. ⏳ Авторизация в админ панели
4. ⏳ CRUD для товаров
5. ⏳ Управление баннерами
6. ⏳ Статистика и учет товара
7. ⏳ Мини-приложение с интеграцией Telegram
8. ⏳ Telegram бот

## 📁 Структура проекта

```
├── apps/
│   ├── admin/          # Админ панель
│   │   ├── src/
│   │   │   ├── app/    # Next.js App Router
│   │   │   └── components/
│   │   └── package.json
│   ├── miniapp/        # Мини-приложение
│   ├── api/            # Backend API
│   │   ├── prisma/     # Схема БД
│   │   └── src/        # Код API
│   └── bot/            # Telegram бот
├── package.json        # Root package.json
└── pnpm-workspace.yaml # Workspace конфигурация
```

## 🛠 Технологии

- **Frontend:** Next.js 14, TypeScript, Tailwind CSS
- **Backend:** Fastify, TypeScript, Prisma ORM
- **Database:** PostgreSQL
- **Telegram:** @twa-dev/sdk, node-telegram-bot-api
- **Monorepo:** pnpm workspaces

## 📚 Документация

- `README.md` - Общее описание проекта
- `PLAN.md` - Детальный план разработки
- `DEVELOPMENT.md` - Руководство по разработке

## 💡 Советы

1. Начните с настройки базы данных - это основа всего проекта
2. Сначала доработайте админ панель - так вы сможете наполнить магазин товарами
3. Затем переходите к мини-приложению - будет что показывать пользователям
4. В конце настройте бота - это финальный штрих

Удачи в разработке! 🚀

